import 'package:flutter/material.dart';

import '../../model/chatuser.dart';

/// Drawer displaying user name, avatar, list of contacts, options and other menu items.
Drawer userDrawer(Stream<ChatUser> stream) {
  return const Drawer();
}